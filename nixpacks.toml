[providers]
python = { version = "3.11" }

[build]
setup = ["pip install -r requirements.txt"]
build = ["python manage.py collectstatic --noinput", "python manage.py migrate"]

[start]
cmd = "/bin/bash start.sh"